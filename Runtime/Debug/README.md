# Debug

- `FPSCounter` writes sampled FPS to a legacy UI Text component.
- `GameLogger` adds consistent context to Console messages and supports per-object muting.

These are lightweight diagnostics, not analytics or save systems. For Stolen's expanded
runtime overlay and event trace, see `Assets/Scripts/DebugTools`.
